# Documentation

Planning and design references for Kanban Studio.

## Contents

| File | Purpose |
|------|---------|
| **`PLAN.md`** | Phased execution plan (Parts 1–10). Includes **implementation notes** where the repo uses an MVP variant (e.g. JSON snapshot in SQLite before normalized tables). |
| **`schema.json`** | Target relational schema: `users`, `boards`, `columns`, `cards` (future migration). |
| **`DATABASE.md`** | Design rationale and migration path from `board_state` JSON. |
| **`PLAN_SIGNOFF.md`** | Baseline sign-off for the plan and schema documentation. |

## Related

- Repo-wide product requirements: **`../AGENTS.md`**
- How to run the app: **`../README.md`**
