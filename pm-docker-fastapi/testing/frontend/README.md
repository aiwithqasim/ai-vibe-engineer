# Frontend Tests

Unit/integration tests (Vitest) and e2e tests (Playwright) for the Next.js frontend.

## Unit tests

From `frontend/`:

```
npm run test:unit
```

## E2e tests

Requires dev server running on port 3000:

```
npm run test:e2e
```
